apiVersion: v1
kind: Service
metadata:
  name: {{ include "claudestruct.fullname" . }}
  labels:
    {{- include "claudestruct.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "claudestruct.selectorLabels" . | nindent 4 }}
